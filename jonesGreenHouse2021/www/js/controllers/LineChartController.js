app.controller("LineChartController", function ($scope) {
/*
  var url = '/sd/jonesGreenHouse/data/';//console.log(url);
      url += $scope.greenhouse_variables.total_days;//console.log(url);
      url += '/powerGenerationData.json';console.log(url);

  $scope.arrData = new Array;
  $scope.arrLabels = new Array;
  $scope.parseDailyGenData = function() {

      $http.get(url).then(function(data){
          angular.forEach(data, function (datapoint) {
            $scope.arrData.push(datapoint.battery_temp_F);
            $scope.arrLabels.push(datapoint.index);
          });

          $scope.data = new Array;
          $scope.labels = new Array;

          $scope.data.push($scope.arrData.slice(0));
          for (var i = 0; i < $scope.arrLabels.length; i++) {
              $scope.labels.push($scope.arrLabels[i]);
          }
          console.log($scope.labels);
      });
  }


  $scope.labels = ["January", "February", "March", "April", "May", "June", "July"];
  $scope.series = ['Series A', 'Series B'];
  $scope.data = [
    [65, 59, 80, 81, 56, 55, 40],
    [28, 48, 40, 19, 86, 27, 90]
  ];
  $scope.onClick = function (points, evt) {
    console.log(points, evt);
  };
  $scope.datasetOverride = [{ yAxisID: 'y-axis-1' }, { yAxisID: 'y-axis-2' }];
  $scope.options = {
    scales: {
      yAxes: [
        {
          id: 'y-axis-1',
          type: 'linear',
          display: true,
          position: 'left'
        },
        {
          id: 'y-axis-2',
          type: 'linear',
          display: true,
          position: 'right'
        }
      ]
    }
  };*/
});
